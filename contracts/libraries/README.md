# Libraries

Shared Solidity utility libraries.

## Files to Implement

| File | Purpose |
|------|---------|
| `CardLib.sol` | Card encoding (uint8: 4 bits suit + 4 bits rank), commitment creation/verification, deck utilities. |
| `BettingLib.sol` | Bet validation (min raise, max stack), blind posting, pot calculation, side pot splitting. |
